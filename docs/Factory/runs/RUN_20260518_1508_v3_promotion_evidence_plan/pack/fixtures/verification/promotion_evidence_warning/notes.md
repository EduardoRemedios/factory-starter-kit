# Promotion Evidence Warning Fixture Notes

This fixture is a planning aid only. It describes the smallest future real-doc drift shape that should trigger promotion-evidence review without authorizing matcher changes.

The future pilot must remove any temporary unsafe wording from real docs before commit and prove final `docs/Factory/v3` returns `ADVISORY_PASS`.

