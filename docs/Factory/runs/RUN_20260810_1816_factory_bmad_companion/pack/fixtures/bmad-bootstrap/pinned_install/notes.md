## Version
- v1
## Change Log
- v1 (2026-08-10): Pinned the synthetic install-plan contract.
## Notes
- `core` is bundled with the installer; `bmm` is the selected method module.
- The exact runtime command is revalidated before implementation.
