# Raw Brief — Factory-BMAD 0.2.5 Solution-Context Integration

## Version
v1

## Change Log
- v1 (2026-09-02): Recorded the human-authorized planning brief for isolated 0.2.5 integration.

## Authorization and Posture
- Human authorization: create a fresh isolated worktree from commit `7f4b6b15c96eb1c7fbbf330f1b0f3855cce5abf6` on branch `codex/factory-bmad-0.2.5-solution-context` and prepare a new Factory integration pack in `PLANNING_ONLY` mode.
- This brief authorizes planning artifacts only. It does not authorize implementation, generated-package replacement, testing that mutates donor state, MS-06, AuditEdge access, Git commit/merge/push, publication, or rollout.

## Problem
The qualified solution-context work exists in `/Users/eduardodosremedios/factory-bmad-0.2.2-repair` as an uncommitted Factory-BMAD `0.2.3` change set with deterministic MS-05 corrective evidence. The intended first-team candidate is Factory-BMAD `0.2.5` in `/Users/eduardodosremedios/factory-starter-kit-0.2.3-uplift`, which contains separate uncommitted nested-layout hardening and retains later cache-integrity and operator-label behavior. Running disposable MS-06 qualification against only the 0.2.3 repair would not qualify the actual 0.2.5 rollout candidate.

## Goal
Prepare an exact, fail-closed implementation and verification contract for semantically integrating the two authored donor change sets onto the clean 0.2.5 base without importing generated packages, overwriting user work, weakening Factory/Conductor authority, or overclaiming readiness.

## Required Integration Outcome
- Preserve Factory-BMAD plugin identity `0.2.5` and Factory dependency `~0.2.5`.
- Preserve 0.2.5 cache-integrity, approval-label, direct Factory-command compatibility, rollout guidance, and nested-install evidence behavior.
- Integrate the qualified responsibility-based solution-context lane: discovery plus exact-version candidate architecture, UX, and spec authoring remain `SOLUTION_CONTEXT` / `EVIDENCE_ONLY` until explicit human promotion and Factory adjudication.
- Preserve default-deny unknown/prohibited workflows and deny every BMAD workflow in unsafe active layouts.
- Preserve the stable public non-canonical-layout reason while exposing granular layout cause as subordinate evidence where required.
- Preserve immutable multi-file promotion, provenance, supersession, claim-disposition, canonical-layout, index-exclusion, and no-authority semantics.
- Merge authored source, tests, fixtures, and necessary operator documentation semantically. Never merge or copy generated package bytes from either donor.
- Regenerate the two Factory-BMAD packages only from the reconciled authored 0.2.5 source after authored gates pass.

## Donor Evidence
- Clean integration base: current worktree HEAD `7f4b6b15c96eb1c7fbbf330f1b0f3855cce5abf6`.
- 0.2.5 donor: `/Users/eduardodosremedios/factory-starter-kit-0.2.3-uplift`; read-only working-tree evidence only.
- Qualified solution-context donor: `/Users/eduardodosremedios/factory-bmad-0.2.2-repair`; read-only working-tree evidence plus `/Users/eduardodosremedios/factory-bmad-solution-context-evidence/RUN_20260830_0808_factory_bmad_solution_context/MS-05-CORRECTION/`.
- Existing checkouts, donor Git state, detached pilot candidates, and worktree registrations are protected no-touch inputs.

## Acceptance Criteria
- AC-01: The pack identifies exact authored collision, donor-only, generated, documentation, test, and protected paths with bounded budgets.
- AC-02: The implementation plan preserves 0.2.5 identity and later safeguards while integrating solution-context behavior without copying generated output.
- AC-03: Unknown, delivery, loop, quick-dev, sprint, code-review-gate, TEA authority, unattended implementation, and unsafe-layout workflow paths remain fail-closed.
- AC-04: Allowed solution-context workflows require exact BMAD 6.10.0 capability/digest/override evidence and produce non-binding context only.
- AC-05: The plan verifies stable public error compatibility and granular unsafe-layout evidence, including direct Factory-command coexistence.
- AC-06: Promotion, preflight, claim dispositions, supersession, legacy-index exclusion, and source/generated parity have deterministic coverage.
- AC-07: One authorized builder invocation occurs only after authored tests pass; generated topology and ownership are verified afterward.
- AC-08: Complete pre/post inventories prove donor checkouts, Factory Core, dependencies, Git/config, application paths, and unrelated files unchanged.
- AC-09: The combined 0.2.5 candidate must pass focused tests, complete regression, package-current/parity, privacy, no-bytecode, knowledge lint, stage/pack lint, and status-ceiling checks before it can be proposed for MS-06.
- AC-10: Completion grants no MS-06, AuditEdge, commit, publication, push, pilot, or rollout authority.

## Non-goals
- Do not implement during this planning run.
- Do not change Factory Core or introduce a universal tool gateway, filesystem sandbox, stage-aware hook framework, configurable active BMAD roots, dependency, or new plugin surface.
- Do not clean, prune, archive, move, copy into, or otherwise mutate any existing checkout or registration.
- Do not reconcile video-branch work, historical pilot worktrees, or unrelated research/product artifacts.
- Do not invoke BMAD workflows or access AuditEdge.
- Do not perform MS-06 disposable qualification in this run.

## Principles
- Authority is determined by evidence and explicit gates, not workflow labels or personas.
- Exact-version proof is non-transitive across plugin or BMAD versions.
- Authored source is canonical; generated packages are replaceable outputs.
- Prefer semantic reconciliation over patch copying where donors overlap.
- Fail closed on ambiguous provenance, write sets, layouts, capability reachability, or public-contract regressions.
- Human review of the completed I2 pack is required before any execution activation.

## Roles
- Eduardo dos Remedios: sole human approval authority.
- Factory Root Planner: prepares the planning pack only.
- Red/Blue/Purple roles: challenge, harden, and adjudicate the plan as artifact roles rather than organizational job titles.
- Implementation agent: absent until a separate digest-bound execution authorization.

## Open Questions
### BLOCKING
- None for planning. Exact implementation paths, budgets, donor hashes, collision-resolution rules, and commands must be fixed by Stage I2 before execution can be proposed.

### NON-BLOCKING
- Physical cleanup of the stale temporary worktree registration remains outside this run and requires separate authorization.

## Go or No-Go Rule
- Planning Go requires Stage A through I2 PASS and final `pack-lint` PASS in `PLANNING_ONLY`.
- Implementation remains No-Go until Eduardo explicitly authorizes the exact unchanged pack, repository/ref, write set, evidence roots, and execution mode transition.
