# Friction log — Conductor 0.3.0 rehearsal pass one (spike-1, 2026-09-04)

| # | Step | What I expected | What happened | Time lost | Severity |
|---|---|---|---|---|---|
| 1 | Brownfield preview | PLAN_READY | BLOCKED CONDUCTOR_CONFLICT_USER_OWNED on `CLAUDE.md`: the repo has a real project guide there; the pinned bridge conflicts. Resolved by moving the guide into a new `AGENTS.md` (so the managed block composes onto it) and making `CLAUDE.md` exactly `@AGENTS.md`, per the quick start. A pilot user hits this on any repo with a CLAUDE.md. Candidate fix: treat an existing CLAUDE.md like AGENTS.md (compose or migrate), not conflict. | 5 min | slow |
| 2 | Fetch origin before rehearsal | current spike-1 | `Repository not found` (credentials); rehearsal ran on the 27 Aug snapshot, archive branch could not be pushed | n/a | blocker for handover, not for rehearsal |
| 3 | Declare `bmad/_bmad` as active root | doctor canonical; architecture/spec/UX authoring allowed | doctor BOTH_PRESENT (declared root works); but `bmad-architecture` is denied `CONDUCTOR_BMAD_SOLUTION_PROFILE_STATE_INVALID` because the pinned-profile check looks for `.claude/skills/bmad-architecture/SKILL.md` at the repo root while the legacy tree keeps its IDE skills under `bmad/.claude/skills`. Harnesses only load skills from the repo root, so a split installation cannot expose BMAD skills to Claude Code at all without a root-level skills directory. Decision needed: symlink/copy skills to root (then profile digests apply) or accept discovery-only at the declared root. | 10 min | slow |
| 4 | BMAD audit at declared root | READY | BLOCKED `CONDUCTOR_BMAD_LOOP_INSTALLED`: the legacy tree carries the `bmad-loop` module, which policy treats as an intake blocker. Correct per policy; the team must remove or isolate bmad-loop before intake/promotion. Discovery still works. | 0 | blocker for promotion, expected |
| 5 | Live hook decisions on the real tree | lanes as designed | product-brief, party-mode, review, testarch-test-design allowed with context; dev-story and testarch-automate denied naming lane, layout, allowed set, next step. Matches AC-L1, AC-L2, AC-L5. | 0 | none |
| 6 | G1 lint after declaring `adapters.bmad` | PASS | FAIL `CONDUCTOR_CONTRACT_ADAPTER_SCHEMA_MISSING: adapters.bmad`: the adapter config schema ships with the companion and is seeded by `intake`, but intake is blocked here by bmad-loop, so the core lint could not validate the block. Seeded `docs/adapters/bmad/contracts/` and `lane_policy.json` by hand. Fix for 0.3.1: the companion seeds its inert contract files on `doctor`/`bootstrap`, not only on intake, or the core plugin ships adapter schemas it knows about. | 5 min | slow |
| 7 | Pass two: `update` from the archived Factory 0.2.5 install | PLAN_READY 0.2.5 -> 0.3.0 | BLOCKED `CONDUCTOR_INSTALLATION_STATE_MISSING`: 0.3.0 reads `docs/Conductor/installation/INSTALLATION_STATE.json`; the 0.2.5 install wrote `docs/Factory/installation/...` and its 85 managed files under `docs/Factory/**`, `scripts/factory_*`. The key-level compatibility (`factory_version`) exists but the path-level rename does not. Fail-closed, so nothing was touched. Fix for 0.3.1: update must discover the legacy state path and plan a rename-aware migration (delete/move the `docs/Factory` managed set, seed `docs/Conductor`). Pilot impact: none for a repo without a prior Factory install (fresh adoption works, pass one). | 0 | blocker for Factory->Conductor updates only |
| 8 | Pass two: BMAD doctor on the archived layout | non-canonical | BLOCKED `CONDUCTOR_BMAD_NON_CANONICAL_LAYOUT`: root `_bmad` plus the unpromoted legacy tree under `docs/upstream/bmad-legacy-spike-1/` (scanned by the context index) is exactly the layout policy forbids. Correct. | 0 | none |
| 9 | G3 verifier report | all rows verified | Verified all three rows and recomputed every receipt digest. Caught a defect the lint does not: constraint C-003 cites `docs/adapters/bmad/BMAD_POLICY.md`, which the core payload does not seed (intake does). Seeded by hand. Candidate 0.3.1 lint rule: constraint `source` paths must resolve when repo-relative. Also noted the postimage covers only protected roots by design; the verifier closed the gap with `git diff HEAD` (empty). | 0 | slow |
