interface:
  display_name: "Factory BMAD Bootstrap"
  short_description: "Preview pinned BMAD upstream setup"
  default_prompt: "Use $conductor-bmad-bootstrap to preview a safe BMAD 6.10.0 upstream setup."
